<!-- Import header with navbar -->
<%- include("partials/header.ejs") %>
  <!-- Biography page -->
  <div class="biog-frame">
    <div class="biog-text">
      <div class="biog-pic-container">
        <img
          src="images/garyphoto.jpg"
          alt="Photo of Gary by bookcase"
          class="biog-photo"
        />
      </div>
      <% paragraphs.forEach((paragraph) => { %>
        <p><%= paragraph %></p>
      <% }) %>
    </div>
  </div>
<!-- Import footer -->
<%- include("partials/footer.ejs") %>
